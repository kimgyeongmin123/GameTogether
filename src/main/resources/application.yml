spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    defer-datasource-initialization: true

  datasource:
    url: jdbc:h2:mem:testdb
    username: mango

  h2:
    console:
      enabled: true

jwt:
  issuer: mango@naver.com
  secret_key: mango-springboot